## Quickstart
If you want to see functionality, clone this repo and run `node input.js`

## To use

```javascript
  let g = new Gistia();
  g.flatten(<object>);
  g.unflatten(<flat object>);
```

## Tests
```
npm install
npm test
```
